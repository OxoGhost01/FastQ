[Unit]
Description=FastQ Job Queue Worker
After=redis.target network.target

[Service]
Type=forking
ExecStart=/usr/local/bin/fastq daemon start
ExecStop=/bin/kill -TERM $MAINPID
PIDFile=/var/run/fastq.pid
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
